# PRD stub: Case-Digest Optimizer (lab, experiment #2) — the founder's lever

Idea (Can, 2026-07-16): don't hand-pick what the system reads — build an RSI
loop that LEARNS what to extract. Tonight's A/B showed the judge-side signal
is saturated; this loop therefore points at the CASE side: it learns how to
summarize a motion/brief (which facts, claims, postures matter to a bench),
scored by prediction accuracy on pre-cutoff outcomes, revised from mistakes,
iterated to plateau — the persona-loop pattern applied to input construction.

Status: QUEUED for experiment #2 (needs brief-level documents; PACER budget
per PIVOT.md lab plan). Build via archon-feature-development when the shop
track allows. Claims only via the September sealed exam.
