# Repo-scoped Archon config
# Tiers are managed via the CLI (`archon ai tier set ...`) — do not hand-author
# a tiers: block here; Archon's validator rejects it. See global ~/.archon/config.yaml.
worktree:
  copyFiles:
    - openai_key.txt
    - token.txt
    - data/persona_liman.md
    - data/persona_furman.md
    - data/persona_subramanian.md
