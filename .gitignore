# Secrets — never in the public pre-registration repo
.env
token.txt
openai_key.txt
anthropic_key.txt

# Raw pulled data (public court records, but large; regenerate via pipeline)
data/*.jsonl

# Local artifacts
__pycache__/
*.pyc

# Product IP — learned extraction instruction sets + digest caches
data/extractor_*.md
data/digests/

# Publish derived, reproducible artifacts (public court data) — keep raw bulk + personas private
!data/labeled.jsonl
!data/exam.jsonl
!data/grounding.jsonl
!data/hand_check_items.json
!data/hand_check_key.json
!data/leakage_audit.json
!data/persona_history.jsonl
data/persona*.md
!data/predictions.jsonl
!data/scorecard.json

# Exam #2 freeze-bound artifacts (personas stay private; hashes bind them)
!data/exam2.jsonl
!data/grounding2.jsonl
!data/labeled2.jsonl
